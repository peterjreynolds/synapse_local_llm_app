# Synapse AI APK

`Synapse-AI.apk` is the latest APK built from `main`.

This branch is force-replaced by GitHub Actions so it exposes one
canonical APK without retaining old APK commits on the branch.
