# Synapse AI APK

`Synapse-AI.apk` is the latest debug APK built from `main`.

This folder is a narrow distribution exception so Peter can download the APK
from the repository file browser on Android. Other generated APKs remain
excluded from git.
