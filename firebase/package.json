{
  "name": "synapse-chat-firebase-policy-tests",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test --test-concurrency=1 tests/*.test.mjs",
    "test:emulators": "firebase emulators:exec --project demo-synapse-chat --only firestore,storage 'npm --prefix firebase test'"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "5.0.1",
    "firebase": "12.16.0"
  }
}
