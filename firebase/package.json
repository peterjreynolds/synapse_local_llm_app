{
  "name": "synapse-chat-firebase-policy-tests",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test --test-concurrency=1 tests/firestore.rules.test.mjs tests/storage.rules.test.mjs",
    "test:registration": "node --test --test-concurrency=1 tests/registration.test.mjs",
    "test:emulators": "npm --prefix functions run build && npm run test:rules:emulator && npm run test:registration:emulator && npm run test:owner:emulator && npm run test:privacy:emulator && npm run test:groups:emulator && npm run test:messages:emulator && npm run test:direct-calls:emulator && npm run test:attachments:emulator",
    "test:attachments": "node --test --test-concurrency=1 tests/attachments.test.mjs",
    "test:attachments:emulator": "GOOGLE_CLOUD_PROJECT=demo-synapse-chat firebase emulators:exec --project demo-synapse-chat --only auth,firestore,functions,storage 'npm run test:attachments'",
    "test:groups": "node --test --test-concurrency=1 tests/groupRooms.test.mjs",
    "test:groups:emulator": "GOOGLE_CLOUD_PROJECT=demo-synapse-chat firebase emulators:exec --project demo-synapse-chat --only auth,firestore,functions 'npm run test:groups'",
    "test:messages": "node --test --test-concurrency=1 tests/richMessages.test.mjs",
    "test:messages:emulator": "GOOGLE_CLOUD_PROJECT=demo-synapse-chat firebase emulators:exec --project demo-synapse-chat --only auth,firestore,functions 'npm run test:messages'",
    "test:direct-calls": "node --test --test-concurrency=1 tests/directCalls.test.mjs",
    "test:direct-calls:emulator": "GOOGLE_CLOUD_PROJECT=demo-synapse-chat firebase emulators:exec --project demo-synapse-chat --only auth,firestore,functions 'npm run test:direct-calls'",
    "test:owner": "node --test --test-concurrency=1 tests/ownerAdmin.test.mjs",
    "test:owner:emulator": "GOOGLE_CLOUD_PROJECT=demo-synapse-chat firebase emulators:exec --project demo-synapse-chat --only auth,firestore,functions 'npm run test:owner'",
    "test:privacy": "node --test --test-concurrency=1 tests/privacyAdmin.test.mjs",
    "test:privacy:emulator": "GOOGLE_CLOUD_PROJECT=demo-synapse-chat firebase emulators:exec --project demo-synapse-chat --only auth,firestore,functions 'npm run test:privacy'",
    "test:registration:emulator": "GOOGLE_CLOUD_PROJECT=demo-synapse-chat firebase emulators:exec --project demo-synapse-chat --only auth,firestore,functions 'npm run test:registration'",
    "test:rules:emulator": "firebase emulators:exec --project demo-synapse-chat --only firestore,storage 'npm test'"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "5.0.1",
    "firebase": "12.16.0",
    "firebase-admin": "^14.1.0",
    "firebase-tools": "15.23.0"
  }
}
